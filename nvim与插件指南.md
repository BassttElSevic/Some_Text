# nvim 与插件使用指南

> 这份文档对应 `~/.config/nvim` 里那套配置（Neovim 0.12.3，lazy.nvim 管 25 个插件）。
> 从 vim 本体的操作讲到每个插件怎么用，按需查。
> 搭建过程和踩的坑记在同目录的另一篇《Neovim 从一行报错到全套开发环境》里。

写于**2026年8月31日-星期一**

---

## 目录

- 第一部分：vim 本体（第 1 到 12 节）
- 第二部分：本配置的基础设定（第 13 节）
- 第三部分：插件逐个讲（第 14 到 31 节）
- 第四部分：内置 treesitter 与配色（第 32 到 33 节）
- 第五部分：速查、常见任务、维护（第 34 到 36 节）

---

插件都是锦上添花，本体操作才是每天用得最多的。这部分不涉及任何插件。

## 1. 模式

vim 的核心设计是分模式。同一个键在不同模式下做不同的事。

| 模式 | 进入方式 | 干什么 | 状态栏显示 |
| --- | --- | --- | --- |
| 普通模式 | `Esc` | 移动、删除、复制，敲命令 | 无 |
| 插入模式 | `i` `a` `o` 等 | 打字 | `-- INSERT --` |
| 可视模式 | `v` `V` `Ctrl+v` | 选中文本 | `-- VISUAL --` |
| 命令行模式 | `:` `/` `?` | 执行命令、搜索 | 底部出现 `:` 或 `/` |
| 终端模式 | 终端窗口里 | 和终端交互 | `-- TERMINAL --` |

**进入插入模式的几种方式**（区别在光标落点）：

| 键 | 效果 |
| --- | --- |
| `i` | 在光标**前**插入 |
| `a` | 在光标**后**插入 |
| `I` | 跳到行首第一个非空字符处插入 |
| `A` | 跳到行尾插入 |
| `o` | 在**下方**新开一行并插入 |
| `O` | 在**上方**新开一行并插入 |
| `gi` | 回到上次退出插入模式的位置继续插入 |

**回普通模式**：`Esc`，或者 `Ctrl+[`（手不用离开主键区）。

**可视模式三种**：

| 键 | 选择方式 |
| --- | --- |
| `v` | 按字符选 |
| `V` | 按整行选 |
| `Ctrl+v` | 按矩形块选（列编辑用这个） |
| `gv` | 重新选中上次选过的区域 |

---

## 2. 移动

### 2.1 基本方向

```text
      k
      ↑
 h ←     → l
      ↓
      j
```

`h` 左、`j` 下、`k` 上、`l` 右。前面加数字表示重复：`5j` 下移 5 行。

### 2.2 按词移动

| 键 | 效果 |
| --- | --- |
| `w` | 到下一个词首 |
| `b` | 到上一个词首 |
| `e` | 到当前/下一个词尾 |
| `ge` | 到上一个词尾 |
| `W` `B` `E` | 同上，但把「词」定义得更宽（以空白分隔，`foo-bar` 算一个） |

### 2.3 行内移动

| 键 | 效果 |
| --- | --- |
| `0` | 行首（第 0 列） |
| `^` | 行首第一个非空字符 |
| `$` | 行尾 |
| `g_` | 行尾最后一个非空字符 |
| `f<字符>` | 向后跳到本行下一个该字符上 |
| `F<字符>` | 向前跳 |
| `t<字符>` | 向后跳到该字符**前一位** |
| `T<字符>` | 向前跳到该字符后一位 |
| `;` | 重复上次 `f`/`t` |
| `,` | 反向重复上次 `f`/`t` |

`f` 系列非常高频。比如要删到本行的逗号：`dt,`。

### 2.4 跨屏移动

| 键 | 效果 |
| --- | --- |
| `gg` | 文件开头 |
| `G` | 文件末尾 |
| `<行号>G` | 跳到指定行，如 `42G` |
| `:<行号>` | 同上，如 `:42` |
| `Ctrl+d` / `Ctrl+u` | 下/上翻半屏 |
| `Ctrl+f` / `Ctrl+b` | 下/上翻整屏 |
| `Ctrl+e` / `Ctrl+y` | 下/上滚一行（光标尽量不动） |
| `H` `M` `L` | 跳到当前屏幕的顶部/中间/底部 |
| `zz` | 把当前行滚到屏幕正中 |
| `zt` / `zb` | 把当前行滚到屏幕顶部/底部 |

### 2.5 段落与括号

| 键 | 效果 |
| --- | --- |
| `{` / `}` | 上/下一个空行（段落边界） |
| `%` | 在配对的括号之间跳转 |
| `(` / `)` | 上/下一句 |

### 2.6 跳转历史

| 键 | 效果 |
| --- | --- |
| `Ctrl+o` | 回到上一个跳转位置 |
| `Ctrl+i` | 前进到下一个跳转位置 |
| `` `` `` | 在当前位置和上次位置之间来回切 |
| `` `. `` | 跳到上次修改的位置 |

写代码时 `Ctrl+o` 用得极多：用 `gd` 跳到定义看一眼，`Ctrl+o` 回来。

### 2.7 标记

| 键 | 效果 |
| --- | --- |
| `m<字母>` | 在当前位置打标记，如 `ma` |
| `` `<字母> `` | 跳到该标记的精确位置 |
| `'<字母>` | 跳到该标记所在行的行首 |
| `:marks` | 列出所有标记 |

小写字母标记只在本文件有效，大写字母（`mA`）跨文件有效。

---

## 3. 编辑：操作符 + 动作

这是 vim 最核心的概念，理解了就能自己组合出无数命令。

**公式**：`操作符` + `动作或文本对象`

常用操作符：

| 操作符 | 含义 |
| --- | --- |
| `d` | 删除（delete） |
| `c` | 删除并进入插入模式（change） |
| `y` | 复制（yank） |
| `p` | 粘贴（put） |
| `>` `<` | 增加/减少缩进 |
| `=` | 自动缩进 |
| `gu` `gU` | 转小写/大写 |
| `gc` | 注释切换（**Neovim 0.10+ 内置**，不需要插件） |

组合示例：

| 命令 | 效果 |
| --- | --- |
| `dw` | 删到下一个词首 |
| `d$` | 删到行尾 |
| `dd` | 删整行 |
| `3dd` | 删 3 行 |
| `cw` | 改一个词 |
| `ct,` | 改到逗号之前 |
| `yy` | 复制整行 |
| `>>` | 当前行缩进一级 |
| `gcc` | 注释/取消注释当前行 |
| `gc3j` | 注释当前行和下面 3 行 |
| `gUiw` | 把光标所在词转成大写 |

---

## 4. 文本对象

文本对象是「动作」的一种，指一块有结构的文本。写法是 `i`（inner，内部）或 `a`（around，含边界）加类型。

| 对象 | 含义 |
| --- | --- |
| `iw` / `aw` | 一个词 / 一个词加旁边空格 |
| `is` / `as` | 一句 |
| `ip` / `ap` | 一段 |
| `i"` / `a"` | 双引号内 / 含引号 |
| `i'` `i` `` ` `` | 单引号、反引号，同理 |
| `i(` `i)` `ib` | 圆括号内 |
| `i{` `i}` `iB` | 花括号内 |
| `i[` `i]` | 方括号内 |
| `i<` `i>` | 尖括号内 |
| `it` / `at` | HTML/XML 标签内 / 含标签 |

组合起来非常好用：

| 命令 | 效果 |
| --- | --- |
| `ci"` | 改双引号里的内容（光标在引号内任意位置都行） |
| `di(` | 删括号里的内容 |
| `ya{` | 复制整个花括号块（含括号） |
| `vip` | 选中当前段落 |
| `dat` | 删掉整个 HTML 标签 |

**这套配置额外提供的文本对象**：

- vimtex 在 `.tex` 文件里加了 `ie`/`ae`（environment 环境）、`i$`/`a$`（行内公式）、`ic`/`ac`（命令）
- flash.nvim 的 `S` 可以按语法节点选择

---

## 5. 复制粘贴与寄存器

vim 的复制不进系统剪贴板，而是进「寄存器」。

| 键 | 效果 |
| --- | --- |
| `yy` | 复制当前行 |
| `y<动作>` | 复制指定范围，如 `y$` 复制到行尾 |
| `p` | 在光标后粘贴 |
| `P` | 在光标前粘贴 |
| `x` | 删除一个字符（进寄存器） |
| `"+y` | 复制到**系统剪贴板** |
| `"+p` | 从系统剪贴板粘贴 |
| `"0p` | 粘贴最近一次 yank 的内容（不受删除影响） |
| `"ayy` | 复制到名为 a 的寄存器 |
| `"ap` | 从 a 寄存器粘贴 |
| `:registers` | 查看所有寄存器内容 |

**要注意的点**：`d`、`c`、`x` 都会覆盖默认寄存器。所以「复制一段，删掉另一段，再粘贴」会失败 —— 粘出来的是刚删的。解决办法有两个：用 `"0p`（专存 yank 的寄存器），或者复制时就存到命名寄存器 `"ayy` 再 `"ap`。

**系统剪贴板**：本机是 Wayland（niri），nvim 需要 `wl-copy`/`wl-paste`（属于 `wl-clipboard` 包）才能和系统剪贴板互通。用 `:checkhealth clipboard` 看当前状态。

在插入模式里粘贴系统剪贴板：`Ctrl+r` 然后 `+`。

---

## 6. 撤销与重做

| 键 | 效果 |
| --- | --- |
| `u` | 撤销 |
| `Ctrl+r` | 重做 |
| `U` | 撤销整行的所有改动 |
| `g-` / `g+` | 在**撤销树**上前后移动（比 `u` 更细） |
| `:earlier 5m` | 回到 5 分钟前的状态 |
| `:later 30s` | 前进 30 秒 |

一次插入模式的全部输入算一次撤销单位。所以长时间不按 `Esc` 地打字，一个 `u` 会撤销掉一大片。想切细一点，中途按几次 `Esc`。

---

## 7. 搜索与替换

### 7.1 搜索

| 键 | 效果 |
| --- | --- |
| `/关键词` | 向下搜索 |
| `?关键词` | 向上搜索 |
| `n` | 下一个匹配 |
| `N` | 上一个匹配 |
| `*` | 搜索光标下的词（向下） |
| `#` | 搜索光标下的词（向上） |
| `:noh` | 清掉搜索高亮 |

搜索支持正则。`\<词\>` 表示全词匹配，`\c` 强制忽略大小写，`\C` 强制区分。

### 7.2 替换

```vim
:s/旧/新/          " 当前行第一个
:s/旧/新/g         " 当前行全部
:%s/旧/新/g        " 全文件全部
:%s/旧/新/gc       " 全文件，每处都问一次（c = confirm）
:'<,'>s/旧/新/g    " 只在可视模式选中的范围内
:5,20s/旧/新/g     " 第 5 到 20 行
```

`:%s//新/g`（搜索词留空）会复用上次搜索的模式，配合 `*` 很顺手：光标放在词上按 `*`，然后 `:%s//新的/g`。

---

## 8. 窗口、buffer、tab

三个概念别混：

- **buffer**：打开的文件在内存里的副本。打开 10 个文件就有 10 个 buffer。
- **窗口（window）**：显示 buffer 的视口。一个 buffer 可以同时显示在多个窗口。
- **tab（标签页）**：窗口的布局集合。不是「打开的文件列表」。

### 8.1 buffer

| 命令 | 效果 |
| --- | --- |
| `:ls` 或 `:buffers` | 列出所有 buffer |
| `:b <编号或名字片段>` | 切到指定 buffer |
| `:bn` / `:bp` | 下一个/上一个 buffer |
| `:bd` | 关闭当前 buffer |
| `Ctrl+^` | 在当前和上一个 buffer 之间切换 |

本配置里更常用 `<空格>fb`（telescope 搜 buffer）和 `<空格>be`（neo-tree 的 buffer 视图）。

### 8.2 窗口

| 键 | 效果 |
| --- | --- |
| `Ctrl+w s` | 水平分屏 |
| `Ctrl+w v` | 垂直分屏 |
| `Ctrl+w h/j/k/l` | 切到左/下/上/右侧窗口 |
| `Ctrl+w w` | 循环切换窗口 |
| `Ctrl+w q` | 关闭当前窗口 |
| `Ctrl+w o` | 只保留当前窗口 |
| `Ctrl+w =` | 所有窗口等宽等高 |
| `Ctrl+w +/-` | 增/减高度 |
| `Ctrl+w </>` | 减/增宽度 |
| `Ctrl+w H/J/K/L` | 把当前窗口移到最左/下/上/右 |
| `Ctrl+w T` | 把当前窗口拆成新 tab |

命令写法：`:sp 文件` 水平分屏打开，`:vs 文件` 垂直分屏打开。

### 8.3 tab

| 命令 | 效果 |
| --- | --- |
| `:tabnew` | 新 tab |
| `gt` / `gT` | 下一个/上一个 tab |
| `<数字>gt` | 跳到第 n 个 tab |
| `:tabc` | 关闭当前 tab |

---

## 9. 保存与退出

| 命令 | 效果 |
| --- | --- |
| `:w` | 保存 |
| `:w 文件名` | 另存为 |
| `:wa` | 保存所有 |
| `:q` | 退出（有未保存改动会拒绝） |
| `:q!` | 强制退出，丢弃改动 |
| `:wq` 或 `:x` | 保存并退出 |
| `:qa` | 退出所有窗口 |
| `:qa!` | 强制全退 |
| `ZZ` | 普通模式下等于 `:wq` |
| `ZQ` | 普通模式下等于 `:q!` |

`:w !sudo tee %` 是「忘了用 sudo 打开」时的救命写法（把当前内容通过 sudo 写回去）。

---

## 10. 宏

录一段操作，然后重复执行。处理重复性编辑非常高效。

| 键 | 效果 |
| --- | --- |
| `q<字母>` | 开始录制到该寄存器，如 `qa` |
| `q` | 停止录制 |
| `@<字母>` | 执行宏，如 `@a` |
| `@@` | 重复上次执行的宏 |
| `<数字>@<字母>` | 执行 n 次，如 `10@a` |

典型用法：给 10 行每行末尾加分号。

```text
qa        开始录制到 a
A;<Esc>   跳到行尾加分号，回普通模式
j         下移一行
q         停止录制
9@a       再执行 9 次
```

---

## 11. 折叠

| 键 | 效果 |
| --- | --- |
| `zc` | 折叠当前 |
| `zo` | 展开当前 |
| `za` | 切换折叠状态 |
| `zR` | 展开全部 |
| `zM` | 折叠全部 |
| `zj` / `zk` | 跳到下/上一个折叠 |

本配置没开自动折叠。想用基于语法结构的折叠，可以在需要时手动设：

```vim
:setlocal foldmethod=expr foldexpr=v:lua.vim.treesitter.foldexpr()
```

---

## 12. 帮助系统

这是 vim 最被低估的部分。任何疑问优先查内置帮助，比搜索引擎准确。

| 命令 | 效果 |
| --- | --- |
| `:help` | 帮助首页 |
| `:help <主题>` | 查具体主题，如 `:help :s` |
| `:help 'option'` | 查选项（带引号），如 `:help 'shiftwidth'` |
| `:help i_CTRL-R` | 查插入模式下的 `Ctrl+r` |
| `:helpgrep <关键词>` | 在所有帮助里全文搜索 |
| `Ctrl+]` | 在帮助里跳到光标下的标签 |
| `Ctrl+o` | 跳回来 |

本配置里用 `<空格>fh`（telescope）搜帮助主题更快。

其他自查命令：

| 命令 | 效果 |
| --- | --- |
| `:checkhealth` | 全面体检（剪贴板、LSP、provider 等） |
| `:messages` | 看历史消息（错误一闪而过时用） |
| `:map <键>` | 查某个键绑到了什么 |
| `:verbose map <键>` | 还会告诉你是哪个文件绑的 |
| `:set <选项>?` | 查选项当前值，如 `:set shiftwidth?` |
| `:lua =表达式` | 求值并打印，如 `:lua =vim.o.termguicolors` |

---

## 13. 已经设好的选项

`~/.config/nvim/lua/config/options.lua` 里：

| 选项 | 值 | 含义 |
| --- | --- | --- |
| `number` | true | 显示绝对行号 |
| `tabstop` | 4 | Tab 显示为 4 空格宽 |
| `shiftwidth` | 4 | `>>` / `<<` 缩进 4 空格 |
| `expandtab` | true | 按 Tab 插入空格而不是制表符 |
| `autoindent` | true | 新行继承上一行缩进 |
| `smartindent` | true | 按语法智能缩进 |
| `termguicolors` | true | 真彩色（必须显式开，否则 colorizer 报错） |

`init.lua` 里：

| 设置 | 值 | 说明 |
| --- | --- | --- |
| `mapleader` | 空格 | 文档里写作 `<空格>` |
| `maplocalleader` | `\`（反斜杠） | vimtex 用它，**刻意和 leader 错开** |
| `loaded_netrw` | 1 | 禁用内置文件浏览器（neo-tree 取代） |

---

## 14. lazy.nvim（插件管理器）

所有插件都由它管。

| 命令 | 效果 |
| --- | --- |
| `:Lazy` | 打开管理界面（最常用） |
| `:Lazy update` | 更新所有插件 |
| `:Lazy sync` | 更新 + 装缺的 + 删多余的 |
| `:Lazy clean` | 只删掉配置里已经没有的插件 |
| `:Lazy profile` | 看每个插件的加载耗时 |
| `:Lazy log` | 看最近更新了什么 |
| `:Lazy restore` | 按 `lazy-lock.json` 回滚到锁定版本 |
| `:Lazy health` | 体检 |

管理界面里的按键：`I` 安装、`U` 更新、`X` 删除、`C` 清理、`q` 退出，光标停在插件名上按 `Enter` 看详情。

**加插件的流程**：在 `~/.config/nvim/lua/plugins/` 下新建一个 `.lua` 文件，`return { { "作者/仓库名", ... } }`，存盘重启 nvim，lazy 会自动装。不需要改任何别的文件（`lua/config/lazy.lua` 里用的是 `{ import = "plugins" }`，整个目录自动加载）。

**版本锁定**：`~/.config/nvim/lazy-lock.json` 记录每个插件的 commit。这个文件建议纳入版本管理，换机器时 `:Lazy restore` 就能装出完全一致的环境。

---

## 15. neo-tree（文件树）

侧边栏文件浏览器，也管文件的增删改。

### 15.1 打开与切换视图

| 键 | 效果 |
| --- | --- |
| `<空格>e` | 开/关文件树 |
| `<空格>E` | 打开并**定位到当前正在编辑的文件** |
| `<空格>be` | 切成「已打开的 buffer」列表 |
| `<空格>ge` | 切成「git 改动」视图 |

命令形式：`:Neotree`、`:Neotree reveal`、`:Neotree float`（浮窗）、`:Neotree git_status`。

### 15.2 树里的按键

| 键 | 效果 |
| --- | --- |
| `l` 或 `Enter` | 打开文件 / 展开目录 |
| `h` | 收起目录 |
| `a` | 新建文件（结尾加 `/` 则建目录） |
| `A` | 新建目录 |
| `d` | 删除 |
| `r` | 重命名 |
| `c` | 复制 |
| `x` | 剪切 |
| `p` | 粘贴 |
| `y` | 复制文件名到剪贴板 |
| `s` | 在垂直分屏打开 |
| `S` | 在水平分屏打开 |
| `t` | 在新 tab 打开 |
| `H` | 显示/隐藏隐藏文件 |
| `R` | 刷新 |
| `?` | 显示全部按键 |
| `q` | 关闭树 |
| `<` / `>` | 在 filesystem / buffers / git_status 之间切换 |

### 15.3 本配置的调整

- 宽度 32
- 显示隐藏文件（`hide_dotfiles = false`），但 `.git` 目录永不显示
- 隐藏被 gitignore 的内容
- 树会自动跟随当前编辑的文件（`follow_current_file`）
- 文件变动自动刷新（用 libuv 文件监视，不用手动按 `R`）
- 树是最后一个窗口时自动关闭
- 空格键在树里**不做任何事**（它是 leader，不解绑会被树吃掉）

---

## 16. telescope（模糊搜索）

搜文件、搜内容、搜 buffer、搜帮助、搜快捷键。

### 16.1 按键

| 键 | 搜什么 |
| --- | --- |
| `<空格>ff` | 文件名（含隐藏文件） |
| `<空格>fg` | 文件**内容**（走 ripgrep，实时搜全项目） |
| `<空格>fb` | 已打开的 buffer |
| `<空格>fr` | 最近打开过的文件 |
| `<空格>fh` | 帮助文档主题 |
| `<空格>fk` | **所有快捷键** |

`<空格>fk` 值得特别记住：忘了按键时按它，然后输关键词（比如输 `git` 就能看到所有 git 相关的键）。

### 16.2 搜索窗口里的按键

| 键 | 效果 |
| --- | --- |
| 直接打字 | 过滤 |
| `Ctrl+n` / `Ctrl+p` | 下/上移动选择 |
| `Ctrl+j` / `Ctrl+k` | 同上 |
| `Enter` | 打开 |
| `Ctrl+x` | 在水平分屏打开 |
| `Ctrl+v` | 在垂直分屏打开 |
| `Ctrl+t` | 在新 tab 打开 |
| `Ctrl+u` / `Ctrl+d` | 在预览窗口里上/下翻 |
| `Tab` | 多选（加入选择列表） |
| `Ctrl+q` | 把结果全部送进 quickfix |
| `Esc` | 从插入切到普通模式（还能再按 `Esc` 关闭） |
| `?` | 显示全部按键 |

### 16.3 其他能搜的东西

`:Telescope` 后接 picker 名字，常用的：

```vim
:Telescope git_files        " 只搜 git 跟踪的文件
:Telescope git_commits      " 提交历史
:Telescope git_branches     " 分支
:Telescope diagnostics      " 全项目诊断
:Telescope lsp_references   " 当前符号的所有引用
:Telescope lsp_definitions  " 定义
:Telescope lsp_document_symbols   " 当前文件的符号列表
:Telescope commands         " 所有可用命令
:Telescope registers        " 寄存器内容
:Telescope colorscheme      " 配色预览
:Telescope resume           " 恢复上次的搜索
```

不记得名字就 `:Telescope` 然后按 Tab 补全。

### 16.4 说明

装了 `telescope-fzf-native`（现场编译的 C 扩展），排序比纯 Lua 快很多，大仓库下差别明显。搜内容依赖系统的 `ripgrep`（`/usr/bin/rg`）。

---

## 17. lazygit（git 操作台）

在浮窗里跑 lazygit，做提交、分支、rebase 这类完整操作。

| 键 | 效果 |
| --- | --- |
| `<空格>gg` | 打开 lazygit |
| `<空格>gf` | 打开当前文件所属仓库的 lazygit |
| `<空格>gl` | 仓库提交历史 |
| `<空格>gc` | 当前文件的提交历史 |

lazygit 自己的常用按键（进去之后）：

| 键 | 效果 |
| --- | --- |
| `1`-`5` | 在各个面板间切换（状态/文件/分支/提交/暂存） |
| `空格` | 暂存/取消暂存选中项 |
| `a` | 暂存全部 |
| `c` | 提交 |
| `P` | push |
| `p` | pull |
| `d` | 查看 diff |
| `x` | 打开菜单看当前面板能做什么 |
| `?` | 帮助 |
| `q` | 退出 |

lazygit 的配色由 Noctalia 的模板管，会跟着壁纸变（前提是那个模板启用了）。

---

## 18. gitsigns（行内 git 信息）

和 lazygit 分工：lazygit 是全屏操作台，gitsigns 是行内视图。

### 18.1 左侧标记的含义

| 标记 | 含义 |
| --- | --- |
| `┃` | 新增的行 |
| `┃`（不同颜色） | 修改的行 |
| `_` | 删除 |
| `‾` | 文件开头的删除 |
| `~` | 改动 + 删除 |
| `┆` | 未跟踪的文件 |

### 18.2 按键

| 键 | 效果 |
| --- | --- |
| `]h` | 跳到下一个改动块 |
| `[h` | 跳到上一个改动块 |
| `<空格>hp` | 预览当前改动块的 diff（浮窗） |
| `<空格>hs` | 暂存当前改动块 |
| `<空格>hr` | 回滚当前改动块 |
| `<空格>hS` | 暂存整个文件 |
| `<空格>hR` | 回滚整个文件 |
| `<空格>hb` | 看当前行的完整 blame |
| `<空格>ht` | 开/关行尾常显的 blame |
| `<空格>hd` | 和索引对比（打开 diff 视图） |
| `<空格>hD` | 和上次提交对比 |

可视模式下选中若干行，`<空格>hs` / `<空格>hr` 只对选中的行生效。

### 18.3 命令

```vim
:Gitsigns toggle_signs           " 开关左侧标记
:Gitsigns toggle_deleted         " 显示被删掉的行内容
:Gitsigns toggle_word_diff       " 词级 diff 高亮
:Gitsigns setqflist              " 所有改动进 quickfix
```

---

## 19. LSP（语言服务器）

提供补全、跳转、诊断、重命名。本配置的架构是 mason 装二进制 + Neovim 0.12 原生 `vim.lsp` 接口。

### 19.1 已配的语言

| 语言 | server | 备注 |
| --- | --- | --- |
| C / C++ | clangd | 大项目建议生成 `compile_commands.json` |
| Rust | rust_analyzer | **需要 `Cargo.toml`** 才有完整诊断 |
| Python | basedpyright + ruff | 前者管类型，后者管 lint |
| LaTeX | texlab + harper_ls | chktex 在保存时跑 |
| Markdown | marksman + harper_ls | 前者管结构，后者管语法拼写 |
| Bash / sh | bashls | 自动调用 shellcheck |

### 19.2 快捷键（大部分是 Neovim 0.12 内置的）

| 键 | 效果 | 来源 |
| --- | --- | --- |
| `K` | 悬浮显示文档 | 内置 |
| `grn` | 重命名符号（跨文件） | 内置 |
| `gra` | 代码操作（快速修复、导入等） | 内置 |
| `grr` | 找所有引用 | 内置 |
| `gri` | 找实现 | 内置 |
| `grt` | 跳到类型定义 | 内置 |
| `gO` | 当前文件的符号列表 | 内置 |
| `]d` / `[d` | 下/上一个诊断 | 内置 |
| `Ctrl+s`（插入模式） | 函数签名提示 | 内置 |
| `gd` | 跳到定义 | 本配置加的 |
| `gD` | 跳到声明 | 本配置加的 |
| `<空格>ld` | 诊断列表（loclist） | 本配置加的 |

跳转之后用 `Ctrl+o` 回来。

`gra`（代码操作）是最容易被忽略但很有用的一个：光标停在报错处按它，会列出可用的自动修复，比如 Python 里自动补 import、Rust 里自动加 `use`。

### 19.3 补全怎么用

补全是 Neovim 0.12 内置的（`vim.lsp.completion`），已配成自动触发。打字时候选项自动弹出。

| 键 | 效果 |
| --- | --- |
| `Ctrl+n` / `Ctrl+p` | 在候选项间移动 |
| `Ctrl+y` | 确认选中的候选项 |
| `Ctrl+e` | 取消补全 |
| `Ctrl+x Ctrl+o` | 手动触发一次补全 |

LSP 返回的代码片段（比如函数参数占位）由内置的 `vim.snippet` 展开，跳到下一个占位用 `Tab`（`vim.snippet.jump`）。

### 19.4 诊断

诊断就是那些报错和警告。左侧有符号，行尾有文字。

| 命令 | 效果 |
| --- | --- |
| `]d` / `[d` | 跳到下/上一个 |
| `<空格>ld` | 列成 loclist |
| `<空格>xx` | 用 trouble 打开更好看的列表（见第 25 节） |
| `:lua vim.diagnostic.open_float()` | 在浮窗里看当前行完整信息 |
| `:lua vim.diagnostic.enable(false)` | 临时关掉诊断显示 |
| `:lua vim.diagnostic.enable(true)` | 再打开 |

行尾文字太长挡视线时，可以关掉虚拟文本：`:lua vim.diagnostic.config({virtual_text=false})`。

### 19.5 LSP 自身的管理命令（0.12 新增）

| 命令 | 效果 |
| --- | --- |
| `:lsp` | 看当前 LSP 状态 |
| `:lsp enable <名字>` | 启用某个 server |
| `:lsp disable <名字>` | 停用 |
| `:lsp restart` | 重启（server 卡住时用） |
| `:checkhealth vim.lsp` | LSP 体检 |

查当前 buffer 挂了哪些 server：

```vim
:lua =vim.tbl_map(function(c) return c.name end, vim.lsp.get_clients({bufnr=0}))
```

server 出问题看日志：

```bash
tail -f ~/.local/state/nvim/lsp.log
```

### 19.6 mason（装 server 的工具）

| 命令 | 效果 |
| --- | --- |
| `:Mason` | 打开管理界面 |
| `:MasonInstall <包名>` | 装 |
| `:MasonUninstall <包名>` | 卸 |
| `:MasonUpdate` | 更新注册表 |
| `:MasonLog` | 看安装日志（装失败时用） |

管理界面里：`i` 安装、`X` 卸载、`u` 更新、`Ctrl+f`/`Ctrl+b` 翻页、`g?` 帮助。

**注意包名和 server 名不一样**。mason 用的是 `rust-analyzer`、`lua-language-server`，lspconfig 用的是 `rust_analyzer`、`lua_ls`。`:MasonInstall` 用前者，`ensure_installed` 里写后者。

所有东西装在 `~/.local/share/nvim/mason/`，不碰系统目录。

---

## 20. nvim-lint（zsh 语法检查）

补 LSP 覆盖不到的部分。目前只配了 zsh，因为 zsh 没有可用的语言服务器，而 shellcheck 官方也不支持 zsh。

| 键 | 效果 |
| --- | --- |
| `<空格>ll` | 手动检查当前文件 |

自动触发时机：**打开文件时**和**保存后**。检查的是磁盘上的文件，所以改完要先 `:w` 才能看到新结果。

能查出来的：括号不配对、缺 `fi`/`done`/`esac`、引号不闭合这类**语法**错误。查不出来的：变量没加引号、用了未定义的变量这类**语义**问题（那些要 shellcheck，而它不支持 zsh）。

bash 和 sh 文件不走这里 —— 由 bashls 自动调 shellcheck，功能更全。

---

## 21. conform（格式化）

| 键 | 效果 |
| --- | --- |
| `<空格>cf` | 格式化当前文件 |
| `<空格>cf`（可视模式） | 只格式化选中部分 |

| 命令 | 效果 |
| --- | --- |
| `:ConformInfo` | 看当前文件会用哪个格式化器、有没有装上 |

### 21.1 各语言用什么

| 语言 | 格式化器 | 缩进 |
| --- | --- | --- |
| Lua | stylua | 4 空格（配置里指定的） |
| Python | ruff（先整理 import 再格式化） | 4 空格（PEP 8） |
| sh / bash / zsh | shfmt | 4 空格（配置里指定的） |
| C / C++ | clang-format | **看项目的 `.clang-format`**，没有则 LLVM 的 2 空格 |
| Rust | rustfmt | 4 空格 |
| LaTeX | latexindent | 默认 |
| 其他 | 退回 LSP 自带的格式化 | |

### 21.2 关于 C/C++ 的缩进

clang-format 会向上搜索 `.clang-format` 并遵守它。想让自己的项目用 4 空格，在项目根目录放一个：

```bash
printf 'BasedOnStyle: LLVM\nIndentWidth: 4\n' > .clang-format
```

放在 `~` 下会对所有没有更近配置的项目生效。

### 21.3 没有开保存时自动格式化

格式化会改文件内容，所以刻意只在按 `<空格>cf` 时才做。想改成自动，编辑 `~/.config/nvim/lua/plugins/format.lua`，把最后那行注释解开：

```lua
format_on_save = { timeout_ms = 1000, lsp_format = "fallback" },
```

---

## 22. vimtex（LaTeX）

texlab 负责补全和诊断，vimtex 负责编译、看 PDF、以及 LaTeX 专用的编辑操作。**按键前缀是反斜杠 `\`**（localleader）。

### 22.1 编译与预览

| 键 | 效果 |
| --- | --- |
| `\ll` | 开始/停止**连续编译**（改完存盘就自动重编） |
| `\lv` | 打开 PDF 并跳到光标所在位置（正向搜索） |
| `\lk` | 停止编译 |
| `\lc` | 清理辅助文件（`.aux` `.log` 等） |
| `\lC` | 清理全部（含 PDF） |
| `\le` | 看编译错误列表 |
| `\lo` | 看编译输出 |
| `\li` | 显示编译信息 |
| `\lt` | 打开目录（Table of Contents） |
| `\lT` | 切换目录窗口 |
| `\lq` | 看 quickfix |

命令形式：`:VimtexCompile`、`:VimtexView`、`:VimtexClean`、`:VimtexErrors`、`:VimtexTocOpen`、`:VimtexInfo`。

### 22.2 LaTeX 专用的编辑操作

这些是 vimtex 最有价值的部分。

| 键 | 效果 |
| --- | --- |
| `dse` | 删掉当前 environment 的 `\begin`/`\end`（内容保留） |
| `cse` | 更换 environment 名（比如 `itemize` 改成 `enumerate`） |
| `tse` | 在 `equation` 和 `equation*` 之间切换（加/去星号） |
| `dsc` | 删掉命令（保留参数内容） |
| `csc` | 更换命令名 |
| `ds$` | 删掉行内公式的 `$` |
| `ds{` / `ds(` | 删掉花括号/圆括号 |

**文本对象**：

| 对象 | 含义 |
| --- | --- |
| `ie` / `ae` | environment 内 / 含 `\begin\end` |
| `i$` / `a$` | 行内公式内 / 含 `$` |
| `ic` / `ac` | 命令的参数 / 含命令名 |
| `id` / `ad` | 分隔符内 / 含分隔符 |
| `iP` / `aP` | 章节（section）内 / 含标题 |

组合示例：`dae` 删整个环境、`cie` 改环境内容、`vi$` 选中公式内容。

**移动**：

| 键 | 效果 |
| --- | --- |
| `]]` / `[[` | 下/上一个章节 |
| `]m` / `[m` | 下/上一个 environment 开头 |
| `]M` / `[M` | 下/上一个 environment 结尾 |
| `%` | 在 `\begin` 和 `\end` 之间跳（vimtex 扩展了原生的 `%`） |

**插入模式的便利**：在 `itemize` 环境里按 `Enter` 会自动补 `\item`。

### 22.3 中文文档（重要）

**默认引擎已经设成 XeLaTeX**，直接写中文就能编译。推荐用 `ctexart` 文档类（ctex 宏包本机已装）：

```latex
\documentclass{ctexart}
\begin{document}
\section{中文测试}
这是一段中文文字，包含标点。
\end{document}
```

也可以自己指定字体（本机有 48 个中文字体，包括 Source Han Serif）：

```latex
\usepackage{fontspec}
\setmainfont{Source Han Serif SC}
```

**为什么不能用默认的 pdflatex**：它处理不了 UTF-8 中文，每个汉字都会报 `LaTeX Error: Unicode character 测 (U+6D4B)`。而且它会**一边报错一边产出缺字的 PDF**，看到有 PDF 很容易以为没问题。

**单个文档想换引擎**，在文件开头（前 20 行内）写一行魔法注释，优先级高于默认值：

```latex
% !TEX program = pdflatex
```

可用的值：`pdflatex`、`xelatex`、`lualatex`。

引擎选择的优先级（从高到低）：魔法注释 > 项目里 `.latexmkrc` 的 `$pdf_mode` > 配置里的默认值。

### 22.4 当前状态与配置

- 编译器 latexmk，参数带 `-synctex=1`（正反向跳转的前提）
- **默认引擎 XeLaTeX**（为了中文）
- **不开保存时自动编译**（大文档很慢）；用 `\ll` 开连续编译模式
- PDF 阅读器 **zathura**（已装），viewer 方法用 `zathura_simple`
- vimtex 自己的补全**已关闭**，交给 texlab，避免两套补全打架
- 诊断也交给 texlab / chktex，不重复往 quickfix 塞
- 不自动折叠（折叠很吃性能）
- 版本锁在 v2.18（master 要求 nvim 0.12.4，本机是 0.12.3）

**为什么用 `zathura_simple` 而不是 `zathura`**：后者靠 xdotool（X11 工具）找窗口 ID 来做聚焦，而本机是 niri（Wayland），zathura 跑成原生 Wayland 客户端，xdotool 看不到，结果每次 `\lv` 都报 `Viewer cannot find Zathura window ID!`。simple 版不做窗口管理，而 zathura 自带 D-Bus 单实例（同一文件的第二次调用转发给已有窗口），所以不会开出一堆窗口，正反向跳转也照旧工作。

**反向跳转**（在 PDF 里定位回源码）：zathura 里用 `Ctrl+左键` 点文字，会跳回 nvim 对应行。这靠 vimtex 给 zathura 传的 `-x` 回调参数，已自动配好。

配色：zathura 的主题由 Noctalia 的模板管，会跟着壁纸变。

---

## 23. render-markdown（Markdown 渲染）

在 buffer 里直接把 Markdown 渲染出来，不用切浏览器。

| 键 | 效果 |
| --- | --- |
| `<空格>tm` | 开/关渲染 |

命令：`:RenderMarkdown toggle`、`:RenderMarkdown enable`、`:RenderMarkdown disable`。

渲染的内容：标题带层级色块背景、表格自动对齐、代码块带语言标签和背景、`- [ ]` / `- [x]` 显示成复选框图标、引用块带竖线、列表符号美化。

**只在普通模式渲染**。进插入模式会自动还原成原始 Markdown 方便编辑，退出插入模式又变回渲染态。

依赖 treesitter 的 markdown parser —— Neovim 0.12 内置就带（`/usr/lib/nvim/parser/markdown.so`），不需要装 nvim-treesitter。

---

## 24. mini.surround（包围符号）

给文本加、删、换包围符号（引号、括号、标签）。前缀是 `gs`。

| 键 | 效果 |
| --- | --- |
| `gsa<动作><符号>` | 加包围 |
| `gsd<符号>` | 删包围 |
| `gsr<旧符号><新符号>` | 换包围 |
| `gsf<符号>` / `gsF<符号>` | 找右/左侧的包围符号 |
| `gsh<符号>` | 高亮包围符号 |

示例：

| 操作 | 效果 |
| --- | --- |
| `gsaiw"` | 给光标所在的词加双引号 |
| `gsa$'` | 从光标到行尾加单引号 |
| `gsd"` | 删掉最近的一对双引号 |
| `gsr"'` | 双引号换成单引号 |
| `gsr({` | 圆括号换成花括号 |
| 可视模式选中后 `gsa(` | 给选中部分加圆括号 |

支持的符号：`(` `)` `[` `]` `{` `}` `<` `>` `"` `'` `` ` ``，以及 `t`（HTML 标签）、`f`（函数调用）。

**为什么是 `gs` 而不是默认的 `s`**：`s` 被 flash.nvim 占了（而且 `s` 原本是「替换单个字符」）。`gs` 原本是 sleep 命令，基本没人用。

---

## 25. mini.pairs（自动配对）

打 `(` 自动补 `)`，打 `"` 自动补 `"`，删左括号时自动删右括号。没有按键需要记，装上就生效。

在已有内容前面打开括号时它会智能判断，不该补的时候不补。

想临时不配对，用 `Ctrl+v` 前缀转义下一个字符。

---

## 26. flash（快速跳转）

按 `s` 然后输入 2 个字符，屏幕上所有匹配位置出现标签字母，按对应字母直接跳过去。跨行跨屏都行，比 `f`/`t` 覆盖范围大得多。

| 键 | 模式 | 效果 |
| --- | --- | --- |
| `s` | 普通/可视/操作符 | 跳转 |
| `S` | 普通/可视/操作符 | 按**语法节点**选择（选中整个函数、整个块） |
| `r` | 操作符 | 远程操作（在别处执行一次操作后回来） |
| `R` | 操作符/可视 | 语法搜索 |
| `Ctrl+s` | 命令行 | 搜索时开关 flash |

用法示例：

- 想跳到屏幕另一处的 `foo`：`s` 然后 `fo`，看到标签按下即可
- 想删除远处的一个词：`d` `s` 然后输两个字符定位，会删除从当前位置到那里
- `S` 连按可以逐级扩大选择范围（词 → 表达式 → 语句 → 函数）

`s` 原本的「替换单个字符」功能可以用 `cl` 代替。

---

## 27. nvim-colorizer（色值预览）

把 `#7e53f9`、`rgb(126, 83, 249)` 这类色值直接显示成颜色块。编辑配色文件（比如 `lua/matugen.lua` 里的 base16 调色板）时很直观。

| 命令 | 效果 |
| --- | --- |
| `:ColorizerToggle` | 开/关 |
| `:ColorizerAttachToBuffer` | 对当前 buffer 启用 |
| `:ColorizerDetachFromBuffer` | 对当前 buffer 关闭 |
| `:ColorizerReloadAllBuffers` | 重新加载 |

默认对所有文件类型生效，用背景色块显示。刻意**不**识别 `red`、`blue` 这类英文单词（误伤太多）。

---

## 28. trouble（诊断列表）

比 loclist 好看好用的列表界面，可以导航、可以预览。

| 键 | 效果 |
| --- | --- |
| `<空格>xx` | 全项目诊断 |
| `<空格>xX` | 只看当前文件的诊断 |
| `<空格>xs` | 符号列表（当前文件的函数、类等结构） |
| `<空格>xl` | LSP 的定义/引用（在右侧打开） |
| `<空格>xL` | loclist |
| `<空格>xq` | quickfix |

列表里的按键：

| 键 | 效果 |
| --- | --- |
| `Enter` | 跳到该处 |
| `o` | 跳过去并关闭列表 |
| `j` / `k` | 上下移动（会实时预览） |
| `<Tab>` | 折叠/展开分组 |
| `q` | 关闭 |
| `?` | 帮助 |

有 7 个语言服务器在产诊断，文件多的时候 `<空格>xx` 比逐个 `]d` 跳快得多。

---

## 29. persistence（会话）

按项目保存和恢复会话 —— 打开的文件、窗口布局、光标位置。

| 键 | 效果 |
| --- | --- |
| `<空格>qs` | 恢复**当前目录**上次的会话 |
| `<空格>ql` | 恢复**最近一次**会话（不限目录） |
| `<空格>qd` | 本次退出不保存会话 |

会话是**自动保存**的（退出时），只需要手动恢复。典型用法：在项目目录里 `nvim`，然后 `<空格>qs` 就回到上次的工作状态。

---

## 30. toggleterm（终端）

在 nvim 里开终端。终端是持久的 —— 关掉再打开还是同一个会话，跑着的命令不会断。

| 键 | 效果 |
| --- | --- |
| `Ctrl+\` | 开/关默认终端（最常用） |
| `<空格>tt` | 水平分屏终端（高 15 行） |
| `<空格>tv` | 垂直分屏终端（宽 40%） |
| `<空格>tf` | 浮窗终端（占屏幕 85% x 80%） |
| `<空格>ta` | 所有终端一起开/关 |

### 30.1 终端里的操作

终端窗口默认是**终端模式**，按键会直接传给 shell。要用 vim 的操作（比如翻看历史输出、复制文字）得先回普通模式：

| 键 | 效果 |
| --- | --- |
| `Esc` `Esc` | 回普通模式（本配置加的） |
| `Ctrl+\` `Ctrl+n` | 回普通模式（vim 原生写法） |
| `Ctrl+h/j/k/l` | 直接从终端切到其他窗口（本配置加的） |
| `i` 或 `a` | 从普通模式回到终端模式 |

### 30.2 多个终端

前面加数字可以开多个独立终端：

```text
2<Ctrl+\>     打开/切换第 2 个终端
3<Ctrl+\>     第 3 个
```

### 30.3 命令

```vim
:ToggleTerm                          " 开关
:ToggleTerm direction=float          " 指定形态
:ToggleTerm size=20 direction=horizontal
:TermExec cmd="cargo build"          " 在终端里执行指定命令
:ToggleTermToggleAll                 " 全部开关
```

`:TermExec` 很实用，可以绑成快捷键做一键编译。

用的 shell 是系统默认的（本机是 zsh）。刻意关掉了终端的暗色遮罩，否则和 matugen 的配色叠在一起会发灰。

---

## 31. which-key（按键提示）

按下前缀键停顿一下，自动弹出这个前缀下所有可用的键和说明。**不需要主动调用**。

| 键 | 效果 |
| --- | --- |
| `<空格>` 然后等一下 | 弹出所有 leader 键 |
| `<空格>?` | 显示当前 buffer 的所有按键（含 buffer 局部的） |
| `:WhichKey` | 手动打开 |
| `:WhichKey <前缀>` | 看指定前缀，如 `:WhichKey <leader>g` |

已经给各前缀起了名字，弹窗里会显示分组：

| 前缀 | 分组名 |
| --- | --- |
| `<空格>b` | buffer |
| `<空格>c` | code（格式化） |
| `<空格>f` | find（搜索） |
| `<空格>g` | git |
| `<空格>h` | hunk（git 改动块） |
| `<空格>l` | lsp / lint |
| `<空格>q` | session（会话） |
| `<空格>t` | terminal / toggle |
| `<空格>x` | diagnostics（诊断列表） |
| `gs` | surround（包围符号） |

记不住按键时，这个和 `<空格>fk`（telescope 搜快捷键）是两个最有用的入口。

---

## 32. 内置 treesitter（没装插件也有）

Neovim 0.12 自带 tree-sitter，且内置了 `c`、`lua`、`markdown`、`markdown_inline`、`query`、`vim`、`vimdoc` 七种 parser。打开这些类型的文件会自动启用语法高亮，不需要装 nvim-treesitter。

好处：高亮按真实语法结构来，比正则高亮准确。base16-nvim 定义了 128 组 treesitter 高亮，所以函数名、方法名、参数、内置函数会用不同颜色，而且跟着壁纸变。

手动控制：

```vim
:lua vim.treesitter.start()      " 对当前 buffer 启用
:lua vim.treesitter.stop()       " 停用
:InspectTree                     " 查看语法树（调试用）
:Inspect                         " 看光标处的高亮组名字
```

`:Inspect` 在调配色时很有用 —— 能告诉你光标下的文字用的是哪个高亮组。

**想支持更多语言**（Python、Rust、JS 等）需要装 nvim-treesitter 插件。目前没装，原因是它正处于 `master` 分支冻结、`main` 分支重写的过渡期。

---

## 33. 配色系统怎么用

配色不由 nvim 自己决定，而是跟着 Noctalia 桌面走。

```text
Noctalia 换壁纸/换主题
  → 生成 ~/.config/nvim/lua/matugen.lua
  → 发 SIGUSR1 给 nvim
  → nvim 热重载配色（不用重启）
```

| 操作 | 说明 |
| --- | --- |
| 换壁纸 | nvim 里的颜色自动跟着变，无需操作 |
| `:colorscheme matugen` | 手动重新应用 |
| `:colorscheme darkblue` | 临时换成内置配色 |
| `:colo` | 看当前配色名 |
| `:Telescope colorscheme` | 预览所有可用配色 |

**不要手改 `~/.config/nvim/lua/matugen.lua`** —— 它是 Noctalia 生成的，下次换壁纸就被覆盖。要改配色逻辑，改 Noctalia 那边的模板（`~/.local/state/noctalia/community-templates/neovim/`）。

`~/.config/nvim/colors/matugen.lua` 是配色入口（让 `:colorscheme matugen` 能用），`lua/config/theme.lua` 存配色名和兜底名。这两个可以改。

---

## 34. 全部快捷键速查

leader 是**空格**，localleader 是**反斜杠**。

### 文件与搜索

| 键 | 效果 |
| --- | --- |
| `<空格>e` | 文件树 开/关 |
| `<空格>E` | 文件树 定位当前文件 |
| `<空格>be` | 文件树 buffer 视图 |
| `<空格>ge` | 文件树 git 视图 |
| `<空格>ff` | 搜文件名 |
| `<空格>fg` | 搜文件内容 |
| `<空格>fb` | 搜 buffer |
| `<空格>fr` | 最近文件 |
| `<空格>fh` | 搜帮助 |
| `<空格>fk` | 搜快捷键 |

### git

| 键 | 效果 |
| --- | --- |
| `<空格>gg` | lazygit |
| `<空格>gf` | lazygit（当前文件仓库） |
| `<空格>gl` | 提交历史 |
| `<空格>gc` | 当前文件提交历史 |
| `]h` / `[h` | 下/上一个改动块 |
| `<空格>hp` | 预览改动块 |
| `<空格>hs` / `<空格>hr` | 暂存 / 回滚改动块 |
| `<空格>hS` / `<空格>hR` | 暂存 / 回滚整个文件 |
| `<空格>hb` | 当前行 blame |
| `<空格>ht` | 开关行内 blame |
| `<空格>hd` / `<空格>hD` | 与索引 / 上次提交对比 |

### 代码

| 键 | 效果 |
| --- | --- |
| `K` | 悬浮文档 |
| `gd` / `gD` | 跳定义 / 跳声明 |
| `grn` | 重命名符号 |
| `gra` | 代码操作 |
| `grr` / `gri` / `grt` | 找引用 / 找实现 / 跳类型 |
| `gO` | 文件符号列表 |
| `]d` / `[d` | 下/上一个诊断 |
| `<空格>ld` | 诊断列表 |
| `<空格>cf` | 格式化 |
| `<空格>ll` | 手动 lint |
| `<空格>xx` / `<空格>xX` | 诊断（全项目 / 当前文件） |
| `<空格>xs` | 符号列表 |
| `<空格>xl` | LSP 引用/定义 |
| `<空格>xL` / `<空格>xq` | loclist / quickfix |

### 编辑

| 键 | 效果 |
| --- | --- |
| `gcc` / `gc<动作>` | 注释切换（内置） |
| `gsa` / `gsd` / `gsr` | 加 / 删 / 换包围符号 |
| `s` / `S` | flash 跳转 / 语法节点选择 |

### 终端与会话

| 键 | 效果 |
| --- | --- |
| `Ctrl+\` | 终端 开/关 |
| `<空格>tt` / `<空格>tv` / `<空格>tf` | 水平 / 垂直 / 浮窗终端 |
| `<空格>ta` | 全部终端开关 |
| `Esc` `Esc`（终端内） | 回普通模式 |
| `<空格>qs` / `<空格>ql` / `<空格>qd` | 恢复本项目 / 最近 / 本次不保存 |

### LaTeX（`.tex` 文件内，前缀是反斜杠）

| 键 | 效果 |
| --- | --- |
| `\ll` | 连续编译 开/停 |
| `\lv` | 看 PDF（zathura） |
| `\lc` | 清理辅助文件 |
| `\le` | 编译错误 |
| `\lt` | 目录 |
| `dse` / `cse` | 删 / 换 environment |
| `ie` / `ae` | environment 文本对象 |
| `]]` / `[[` | 下/上一章节 |

### 其他

| 键 | 效果 |
| --- | --- |
| `<空格>tm` | Markdown 渲染开关 |
| `<空格>?` | 显示当前 buffer 全部按键 |

---

## 35. 常见任务怎么做

**在项目里找一个函数的定义**
`<空格>ff` 搜文件打开，或者 `<空格>fg` 直接搜函数名；跳到之后 `gd` 进定义，`Ctrl+o` 回来。

**改一个变量名，全项目同步**
光标停在变量上，`grn`，输入新名字回车。LSP 会改掉所有引用（跨文件）。

**看这行代码是谁写的**
`<空格>hb`。

**只提交当前文件的一部分改动**
`]h` / `[h` 逐个跳改动块，`<空格>hp` 看内容，要的按 `<空格>hs` 暂存；都挑完了 `<空格>gg` 进 lazygit 按 `c` 提交。

**代码有报错，想快速修**
`]d` 跳到报错，`K` 看详情，`gra` 看有没有自动修复。多个报错就 `<空格>xx` 列出来逐个处理。

**格式化整个文件**
`<空格>cf`。想只格式化一部分，可视模式选中再按。

**跑一次编译/测试**
`Ctrl+\` 开终端，敲命令。或者用 `:TermExec cmd="cargo test"` 一步到位。

**写 LaTeX 并实时预览**
`\ll` 开连续编译，`\lv` 打开 PDF。之后每次 `:w` 都会自动重编，PDF 自动刷新。

**写 Markdown**
直接写。渲染是自动的，进插入模式会还原成原始文本。要临时关掉渲染按 `<空格>tm`。

**忘了某个快捷键**
按 `<空格>` 等一下看 which-key 弹窗，或者 `<空格>fk` 搜。

**改完配置想生效**
重启 nvim。装了新插件的话 lazy 会自动安装。

---

## 36. 维护

### 36.1 日常维护命令

| 命令 | 什么时候用 |
| --- | --- |
| `:Lazy` | 看插件状态、手动更新 |
| `:Lazy update` | 定期更新插件 |
| `:Mason` | 看/更新语言服务器 |
| `:checkhealth` | 感觉哪里不对时先跑这个 |
| `:messages` | 有报错一闪而过时 |
| `:Lazy profile` | 觉得启动变慢时 |

### 36.2 加一个新插件

在 `~/.config/nvim/lua/plugins/` 下新建文件，比如 `foo.lua`：

```lua
return {
    {
        "作者/插件名",
        event = "VeryLazy",        -- 什么时候加载
        opts = {},                 -- 传给 setup() 的配置
        keys = {                   -- 快捷键（写这里而不是 config 里）
            { "<leader>xx", "<cmd>Foo<cr>", desc = "说明" },
        },
    },
}
```

重启 nvim，lazy 自动安装。

**加载时机的几种写法**：

| 写法 | 含义 |
| --- | --- |
| `lazy = false` | 启动就加载（配色这类要用） |
| `event = "VeryLazy"` | 启动完成后再加载 |
| `event = { "BufReadPre", "BufNewFile" }` | 打开文件时 |
| `ft = "markdown"` | 只在特定文件类型 |
| `cmd = "Foo"` | 执行该命令时 |
| `keys = { ... }` | 按下该键时 |

**快捷键一定写在 `keys` 里，不要写在 `config` 里** —— 写在 `config` 里的话，插件没加载键就不存在。

### 36.3 出问题的排查顺序

1. `:messages` 看有没有报错被刷掉了
2. `:checkhealth` 全面体检
3. 怀疑是插件冲突：`nvim --clean` 对比，不加载配置还有问题就是系统层面的
4. 怀疑 LSP：`:lsp` 看状态，`tail -f ~/.local/state/nvim/lsp.log` 看日志
5. 怀疑格式化：`:ConformInfo` 看用的是哪个格式化器、有没有装上
6. 怀疑按键冲突：`:verbose map <键>` 看是哪个文件绑的

### 36.4 备份与恢复

配置本身：`~/.config/nvim/`，建议纳入 git（连 `lazy-lock.json` 一起）。

改动前的旧配置备份在 `~/.config/nvim.bak.20260831-030907`。

换机器时：拷 `~/.config/nvim/`，启动 nvim，`:Lazy restore`（按 lockfile 装出一致版本），然后 `:MasonInstall` 那些语言服务器。

**注意**：`lua/matugen.lua` 是 Noctalia 生成的，新机器上如果没有 Noctalia，nvim 会自动回退到内置的 darkblue 配色，不会报错。

### 36.5 待办

- Rust 标准库跳转：`rustup component add rust-src`
- 想给 nvim 配置本身加 LSP：`:MasonInstall lua-language-server`，并配 `diagnostics.globals = { "vim" }`
- nvim 升到 0.12.4 后，删掉 `lua/plugins/latex.lua` 里的 `tag = "v2.18"`
